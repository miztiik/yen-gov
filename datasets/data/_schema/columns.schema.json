{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "./columns.schema.json",
  "title": "CSV column contract (schema-of-schemas)",
  "description": "Validates datasets/data/_schema/columns.json - the one machine-readable home for the per-file-class CSV column contract. Spec: docs/architecture/data/csv-column-contract.md (D-DOC0). Consumed by the backend write-time validator (B1.3), the frontend typed-read codegen (F1), and the cross-format drift test.",
  "x-version": "2.3",
  "x-changelog": [
    {
      "version": "1.0",
      "date": "2026-06-04",
      "description": "Initial sub-plan B1.1 schema for the columns.json artifact. Mirrors csv-column-contract.md sections 2-4: file-class keyed by canonical-tree glob; per-column {name, dtype, nullable, pk?, fk?, enum?, derived?}; closed-enum membership; no second hand-typed column-name copy permitted."
    },
    {
      "version": "1.1",
      "date": "2026-06-10",
      "description": "Companion bump for columns.json $schema_version 1.0 -> 1.1 (PR-0 of TODO/20260610-electoral-data-quality-and-party-catalogue-plan.md). No structural change to the schema-of-schemas itself; pure-additive minor on the data file: 10 nullable identity-metadata columns appended to the datasets/data/entities/parties.csv file-class (recognition_scope, home_state_codes, founded_year, dissolved_year, predecessor_party_ids, successor_party_ids, name_history, claims_to_parent_name, name_native_script, is_sentinel) per Hans + Gregor Wave 0 verdict (plan section 2.1). Existing rows unchanged in shape; 3 sentinel rows (parties.IN.UNK / parties.IN.IND / parties.IN.NOTA) lifted from adapter hardcodes into the CSV so FK closure now includes them (Hans Wave 0 / section 2.1)."
    },
    {
      "version": "2.0",
      "date": "2026-06-12",
      "description": "Companion bump for columns.json $schema_version 1.1 -> 2.0 (Phase 1 of TODO/20260612-alliance-phase-1-structural-fix-plan.md). No structural change to the schema-of-schemas itself; MAJOR breaking on the datasets/data/entities/party_alliances.csv file-class: renamed period_label -> event_id so the column joins citizen-route canonical event_id values (general-2024, assembly-2024, ...); added state column (LGD state slug for state-scoped events, literal IN for national events) to disambiguate per-state cohorts that share an event_id; dropped short_name (Holy Law #6 - readers join through party_id FK to parties.csv.short_name). PK changed from (party_id, period_label) to (party_id, event_id, state). All 61 existing on-disk rows migrated 1:1 in the same commit (no row loss); the 10 TN-AcGenMay2026 rows additionally repointed to a new Wikipedia src_id (src-bca3c60cdafb) for the 2026 Tamil Nadu Legislative Assembly election article (Resolution A; the legacy src-c3e2fd43efa5 row in source.csv stays as the legitimate citation for the 1999-LS data it actually describes)."
    },
    {
      "version": "2.1",
      "date": "2026-06-14",
      "description": "Companion bump for columns.json $schema_version 2.0 -> 2.1 (PR-A of TODO/20260614-three-ephemeral-ingests-plan.md). No structural change to the schema-of-schemas itself; pure-additive minor on the data file: NEW file-class datasets/elections/parliament/election=*/mcc_seizures.csv for the ECI MCC-period daily enforcement-seizures press-note series (national 17th-LS 2019 press-note, 36 states/UTs x 10 dates = 360 rows, composite PK (state_slug, date), 13 columns). Publisher's TOTAL is preserved verbatim (D3 'no derived totals') so total_seizure_inr_crore is NOT derived; consumers MUST NOT recompute it. The 2 pre-2020-merger UTs (Dadra and Nagar Haveli, Daman and Diu) are kept on their historical slugs (dadra-and-nagar-haveli, daman-and-diu) - not the merged dadra-and-nagar-haveli-and-daman-and-diu slug - because the 2019 publisher rows are historically distinct; state_slug in this file-class has no FK declared (per-event publisher-distinct identity, not a state_codes.csv reference). Existing file-classes unchanged in shape."
    },
    {
      "version": "2.2",
      "date": "2026-06-15",
      "description": "Companion bump for columns.json $schema_version 2.1 -> 2.2 (PR-E1 of TODO/20260615-elections-redesign-plan.md). No structural change to the schema-of-schemas itself; pure-additive minor on the data file: NEW file-class datasets/data/marts/elections/event_summary.csv (12 columns, composite PK (event_id, state_code)) for the per-event aggregate emitted by `python -m yen_gov derive-event-summary`. Lives under marts/ alongside the party_pages mart (history.csv, strongholds.csv, manifest.csv) - the established home for derived aggregate CSVs. scope is a 2-value enum (national | state). state_code is nullable AND part of the composite PK (null for scope=national rows, the single Parliament row per event_id; non-null for scope=state rows, the per-Assembly rows). turnout_pct + seats_contested are derived event-scope aggregates of the upstream summary.csv. leading_party_id + runner_up_party_id FK parties.csv.party_id; source_id FK source.csv.source_id. Existing file-classes unchanged in shape."
    },
    {
      "version": "2.3",
      "date": "2026-06-15",
      "description": "Companion bump for columns.json $schema_version 2.2 -> 2.3 (PR-1 of TODO/20260615-party-page-citizen-fixes-plan.md). No structural change to the schema-of-schemas itself; pure-additive minor on the data file: 3 nullable trailing columns appended to datasets/data/entities/parties.csv file-class (source_id FK datasets/data/entities/source.csv.source_id, processing_level enum [minor, major], processing_note free-text) per user-ratified Path A (Scope-change ledger L-4) and the L-1 per-row processing-level doctrine at docs/concepts/data-quality.md. The bump unblocks PR-1 D1 founded_year backfill (952 of 2776 rows previously null) by giving the catalogue a place to record the issuing-authority source_id + the per-row processing receipt per CLAUDE.md Holy Law #9 + section 12 + the OWID-aligned per-row scope (named divergence #6, docs/concepts/owid-alignment.md). All 2779 pre-v1.2 rows ship the 3 trailing fields empty (no semantic change); the 14 priority parties get founded_year + the 3 provenance fields populated via the canonical helpers (backend.yen_gov.canonical.processing_quality.derive_processing_for_party_founded_year_backfill + backend.yen_gov.canonical.citation.derive_source_id) per CLAUDE.md section 10 anti-pattern (NO hand-authored literals). Other file-classes unchanged in shape."
    }
  ],
  "type": "object",
  "additionalProperties": false,
  "required": ["$schema", "$schema_version", "file_classes"],
  "properties": {
      "$schema": { "const": "./columns.schema.json" },
    "$schema_version": { "$ref": "#/$defs/version" },
    "file_classes": {
      "type": "object",
      "minProperties": 1,
      "propertyNames": { "$ref": "#/$defs/file_class_glob" },
      "additionalProperties": { "$ref": "#/$defs/file_class" }
    }
  },
  "$defs": {
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+$"
    },
    "file_class_glob": {
      "type": "string",
      "pattern": "^datasets/(data|elections)/[A-Za-z0-9_./*=<>-]+\\.csv$",
      "description": "Glob over the canonical tree, anchored at repo root. POSIX separators only. No double-underscore in any path segment (plan section 21.6)."
    },
    "file_class": {
      "type": "object",
      "additionalProperties": false,
      "required": ["columns"],
      "properties": {
        "columns": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/column" },
          "description": "Columns in EMISSION order. The CSV header MUST equal this list (no reordering, no extras, no omissions) per csv-column-contract.md section 2."
        },
        "notes": {
          "type": "string",
          "description": "Human-readable note carried alongside the file-class entry. Not consumed by the validator; surfaced in tooling output."
        }
      }
    },
    "column": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "dtype", "nullable"],
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9]*(_[a-z0-9]+)*$",
          "description": "snake_case identifier. No double-underscore."
        },
        "dtype": {
          "type": "string",
          "enum": ["string", "integer", "number", "boolean"],
          "description": "CSV-readable dtype. Maps 1:1 to a DuckDB read_csv() column type."
        },
        "nullable": {
          "type": "boolean",
          "description": "Whether the column may carry NULL. The writer rejects NULL on a non-nullable column; the validator rejects empty-string-on-nullable=false."
        },
        "pk": {
          "type": "boolean",
          "description": "Member of the primary key for this file class. Composite PKs declare pk:true on each member. Rows are sorted deterministically by the PK before write."
        },
        "fk": {
          "type": "string",
          "pattern": "^datasets/(data|elections)/[A-Za-z0-9_./*=<>-]+\\.csv\\.[a-z][a-z0-9_]*$",
          "description": "Foreign-key target as '<file-class-glob>.<column>'. Enforced by the B1.3 validator."
        },
        "enum": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string" },
          "description": "Closed-enum membership (single source per csv-column-contract.md section 4). The validator rejects values outside the set."
        },
        "derived": {
          "type": "boolean",
          "description": "F7 computed at write time, never hand-authored. The writer rejects callers that supply the column in input rows."
        }
      }
    }
  }
}
