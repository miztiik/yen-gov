{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "./elections-candidacies.schema.json",
  "title": "Election candidacy fact row",
  "description": "Logical contract for one row of datasets/elections/elections_candidacies.parquet. ADR-0035 S.1 splits per-contest candidacy facts away from dim_persons while keeping candidacy_key byte-equal to election_results.entity_id for candidate-* observations.",
  "x-version": "1.1",
  "x-changelog": [
    {
      "version": "1.0",
      "date": "2026-05-23",
      "description": "Initial S.1 candidacy fact schema. Adds candidacy_key -> person_id bridge plus per-contest party, rank, vote, and reservation/party-type attributes moved out of dim_candidates."
    },
    {
      "version": "1.1",
      "date": "2026-05-24",
      "description": "Widen candidacy_key and ballot_serial to three digits for large historical candidate fields, anchored by Karnataka 1985 Belgaum."
    }
  ],
  "type": "object",
  "additionalProperties": false,
  "required": [
    "candidacy_key",
    "person_id",
    "ac_id",
    "election_id",
    "ballot_serial",
    "party_id",
    "rank",
    "won",
    "source_id"
  ],
  "properties": {
    "candidacy_key": {
      "type": "string",
      "pattern": "^IN-[SU][0-9]{2}-AC-[0-9]{4}-[0-9]+-(?:AcGen|LsGen|AcBye|LsBye)(?:Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)[0-9]{4}-C[0-9]{2,3}$",
      "description": "PK. The retired candidate_id / current election_results.entity_id for candidate-* observation rows."
    },
    "person_id": {
      "type": "string",
      "pattern": "^[0-9a-f]{16}$",
      "description": "FK -> elections/dim_persons.parquet person_id."
    },
    "ac_id": {
      "type": "string",
      "pattern": "^IN-[SU][0-9]{2}-AC-[0-9]{4}-[0-9]+$",
      "description": "FK -> dim_acs.ac_id."
    },
    "election_id": {
      "type": "string",
      "minLength": 1,
      "description": "ECI event id / period_label, e.g. AcGenApr2021."
    },
    "ballot_serial": {
      "type": "integer",
      "minimum": 1,
      "maximum": 999,
      "description": "Position on the ballot when available; current Section-10 path uses rank as a stand-in."
    },
    "party_id": {
      "type": "string",
      "pattern": "^parties\\.IN\\.[A-Z][A-Z0-9_]*$",
      "description": "FK -> dim_parties.party_id."
    },
    "rank": {
      "type": "integer",
      "minimum": 1,
      "description": "Final vote-rank within the contest. 1 = winner."
    },
    "votes_polled": {
      "type": ["number", "null"],
      "minimum": 0,
      "description": "Candidate votes. Mirrors candidate-votes-polled observation for convenient joins; null only when the source omitted the vote count."
    },
    "vote_share_pct": {
      "type": ["number", "null"],
      "minimum": 0,
      "maximum": 100,
      "description": "Candidate vote share percentage. Mirrors candidate-vote-share-pct observation for convenient joins."
    },
    "won": {
      "type": "boolean",
      "description": "True iff rank = 1 for the contest."
    },
    "source_id": {
      "type": "string",
      "minLength": 1,
      "description": "FK -> taxonomy/sources.parquet. Same ECI citation row as the candidate observations."
    },
    "party_short_raw": {
      "type": ["string", "null"],
      "minLength": 1,
      "description": "Verbatim upstream party short string used as UI fallback when party_id resolves to parties.IN.UNK."
    },
    "constituency_type": {
      "type": ["string", "null"],
      "enum": ["GEN", "SC", "ST", null],
      "description": "Seat reservation category for this candidacy/contest. Null when not declared."
    },
    "party_type": {
      "type": ["string", "null"],
      "enum": [
        "NATIONAL",
        "STATE",
        "OTHER_STATE",
        "REGISTERED_UNRECOGNISED",
        "INDEPENDENT",
        null
      ],
      "description": "ECI 5-bucket party classification for this candidacy. Null when not declared."
    }
  }
}
