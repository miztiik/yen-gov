{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://yen-gov.github.io/schemas/district.schema.json",
  "title": "Districts collection (per state)",
  "description": "All districts within a single state, keyed by LGD (Local Government Directory) code where available, falling back to a Wikipedia-derived slug. Each file covers exactly one state.",
  "x-version": "3.0",
  "x-changelog": [
    {
      "version": "1.0",
      "date": "2026-05-08",
      "description": "Initial schema. Per-state district list with LGD/Wikipedia identifier, name, creation date, split-from lineage, source citation."
    },
    {
      "version": "2.0",
      "date": "2026-05-08",
      "description": "Replaced optional source_url with required source field (top-level) and optional source override on items per provenance contract (CLAUDE.md §12)."
    },
    {
      "version": "3.0",
      "date": "2026-05-08",
      "description": "Replaced single source string with sources array of {url, fetched_at} per ADR-0002. Empty array means hand-authored. Item-level source overrides removed."
    }
  ],
  "type": "object",
  "required": ["$schema", "$schema_version", "sources", "state", "districts"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string", "format": "uri" },
    "$schema_version": { "type": "string", "pattern": "^\\d+\\.\\d+$" },
    "sources": {
      "type": "array",
      "description": "Provenance per CLAUDE.md §12 / ADR-0002. Each entry is one upstream URL plus the timestamp our pipeline fetched it. An empty array signals hand-authored content.",
      "items": {
        "type": "object",
        "required": ["url", "fetched_at"],
        "additionalProperties": false,
        "properties": {
          "url": { "type": "string", "format": "uri", "pattern": "^https?://" },
          "fetched_at": { "type": "string", "format": "date-time" }
        }
      }
    },
    "state": {
      "type": "string",
      "pattern": "^[SU]\\d{2}$",
      "description": "ECI state code that this collection belongs to."
    },
    "districts": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "id_source", "name"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1,
            "description": "Stable identifier. LGD numeric code (preferred) or Wikipedia URL slug (fallback)."
          },
          "id_source": {
            "enum": ["lgd", "wikipedia"],
            "description": "Which authority issued the id."
          },
          "name": { "type": "string", "minLength": 1 },
          "headquarters": { "type": "string" },
          "created_on": {
            "type": ["string", "null"],
            "format": "date",
            "description": "Date the district was constituted, when known."
          },
          "split_from": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Parent district ids (same id_source) if this district was carved out."
          },
          "notes": { "type": "string" }
        }
      }
    }
  }
}
