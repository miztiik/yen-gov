{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://yen-gov.github.io/schemas/district.schema.json",
  "title": "Districts collection (per state)",
  "description": "All districts within a single state, keyed by LGD (Local Government Directory) code where available, falling back to a Wikipedia-derived slug. Each file covers exactly one state.",
  "x-version": "3.3",
  "x-changelog": [
    {
      "version": "1.0",
      "date": "2026-05-08",
      "description": "Initial schema. Per-state district list with LGD/Wikipedia identifier, name, creation date, split-from lineage, source citation."
    },
    {
      "version": "2.0",
      "date": "2026-05-08",
      "description": "Replaced optional source_url with required source field (top-level) and optional source override on items per provenance contract (CLAUDE.md §12)."
    },
    {
      "version": "3.0",
      "date": "2026-05-08",
      "description": "Replaced single source string with sources array of {url, fetched_at} per ADR-0002. Empty array means hand-authored. Item-level source overrides removed."
    },
    {
      "version": "3.1",
      "date": "2026-05-11",
      "description": "Additive: optional name (human-readable upstream name) and authority (issuing body) fields on each sources[] entry. Aligns with Phase A schemas (feature_collection.metadata, indicator) so attribution can be uniform across electoral and socio-economic data. Per TODO/SOCIO-ECONOMIC-EXPANSION.md §6 D3."
    },
    {
      "version": "3.2",
      "date": "2026-05-13",
      "description": "Additive: optional lgd_code (LGD numeric district code, string) on each district item. Set during backfill from datasets/reference/in/lgd/districts-latest.csv (ramSeraph opendata mirror). Independent of id/id_source so wikipedia-slug ids stay stable for cross-file references in constituencies.json. lgd_code is the join key against datasets/boundaries/in/geojson/india-districts.geojson where features carry dist_lgd. Per ADR-0015 (LGD as district authority) and ADR-0019 (district_lgd_code dataset column)."
    },
    {
      "version": "3.3",
      "date": "2026-05-15",
      "description": "Additive: optional name_alt (alternate canonical name — e.g. Thoothukudi/Tuticorin), name_source enum (lgd|census_2011 — which authority issued the canonical name), census_2011_code (Census 2011 district code, frozen at 2011 — null for post-2011 districts), lgd_code_history (array of {old, retired_on} for districts whose LGD code changed), created_after_2011 ({date, parent_lgd_codes:[...], notes} — parent_lgd_codes is plural because some new districts (e.g. Chengalpattu) were carved from multiple parents). Surfaces the methodology break required by trend visualisations across Census-frozen layers vs current-LGD layers. Existing v3.2 data files re-validate unchanged. Per TODO/TN-GRANULAR-GEO-PLAN.md Phase 0 (Hans v2 nits)."
    }
  ],
  "type": "object",
  "required": [
    "$schema",
    "$schema_version",
    "sources",
    "state",
    "districts"
  ],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "format": "uri"
    },
    "$schema_version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+$"
    },
    "sources": {
      "type": "array",
      "description": "Provenance per CLAUDE.md §12 / ADR-0002. Each entry is one upstream URL plus the timestamp our pipeline fetched it. An empty array signals hand-authored content.",
      "items": {
        "type": "object",
        "required": [
          "url",
          "fetched_at"
        ],
        "additionalProperties": false,
        "properties": {
          "url": {
            "type": "string",
            "format": "uri",
            "pattern": "^https?://"
          },
          "fetched_at": {
            "type": "string",
            "format": "date-time"
          },
          "name": {
            "type": "string",
            "description": "Optional human-readable name of the upstream (e.g. \"Statistical Report on General Election to Legislative Assembly of Tamil Nadu, 2021\")."
          },
          "authority": {
            "type": "string",
            "description": "Optional issuing authority (e.g. \"Election Commission of India\")."
          }
        }
      }
    },
    "state": {
      "type": "string",
      "pattern": "^[SU]\\d{2}$",
      "description": "ECI state code that this collection belongs to."
    },
    "districts": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "id",
          "id_source",
          "name"
        ],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1,
            "description": "Stable identifier. LGD numeric code (preferred) or Wikipedia URL slug (fallback)."
          },
          "id_source": {
            "enum": [
              "lgd",
              "wikipedia"
            ],
            "description": "Which authority issued the id."
          },
          "lgd_code": {
            "type": "string",
            "pattern": "^[1-9][0-9]*$",
            "description": "LGD numeric district code (Local Government Directory, Ministry of Panchayati Raj). Optional. Join key against datasets/boundaries/in/geojson/india-districts.geojson features (`dist_lgd` property). When id_source=lgd this duplicates id; when id_source=wikipedia this is the LGD bridge added by tools/lgd/backfill_lgd_codes.py."
          },
          "name": {
            "type": "string",
            "minLength": 1
          },
          "name_alt": {
            "type": "string",
            "minLength": 1,
            "description": "Optional alternate canonical name when the upstream LGD CSV and the citizen-recognised name diverge (e.g. \"Tuticorin\" alongside name \"Thoothukudi\"). Tooltip / search affordance only — never an identifier."
          },
          "name_source": {
            "enum": ["lgd", "census_2011", "wikipedia"],
            "description": "Optional. Which authority issued the canonical name. Defaults to id_source when absent."
          },
          "census_2011_code": {
            "type": ["string", "null"],
            "pattern": "^[0-9]+$",
            "description": "Optional. Census 2011 district code (frozen at the 2011 Census; non-incrementing). Null for post-2011 districts (e.g. Mayiladuthurai 2020, Tenkasi 2019). Lets trend visualisations bridge Census-frozen layers (e.g. SECC) to current LGD geometry."
          },
          "lgd_code_history": {
            "type": "array",
            "description": "Optional. Prior LGD codes this district has held (rare but happens on UT reorganisation, etc.). Most recent code is the current `lgd_code`.",
            "items": {
              "type": "object",
              "required": ["old", "retired_on"],
              "additionalProperties": false,
              "properties": {
                "old": { "type": "string", "pattern": "^[1-9][0-9]*$" },
                "retired_on": { "type": "string", "format": "date" }
              }
            }
          },
          "created_after_2011": {
            "type": "object",
            "description": "Optional. Methodology-break marker for districts that did not exist in Census 2011. Surfaces the break to trend visualisations so they don't draw a continuous polyline through a discontinuity. parent_lgd_codes is plural because some new districts (e.g. Chengalpattu, carved from multiple parents) have more than one ancestor.",
            "required": ["date", "parent_lgd_codes"],
            "additionalProperties": false,
            "properties": {
              "date": { "type": "string", "format": "date" },
              "parent_lgd_codes": {
                "type": "array",
                "minItems": 1,
                "items": { "type": "string", "pattern": "^[1-9][0-9]*$" }
              },
              "notes": { "type": "string" }
            }
          },
          "headquarters": {
            "type": "string"
          },
          "created_on": {
            "type": [
              "string",
              "null"
            ],
            "format": "date",
            "description": "Date the district was constituted, when known."
          },
          "split_from": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Parent district ids (same id_source) if this district was carved out."
          },
          "notes": {
            "type": "string"
          }
        }
      }
    }
  }
}
