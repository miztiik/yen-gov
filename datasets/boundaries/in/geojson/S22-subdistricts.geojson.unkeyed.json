{
  "$schema": "https://yen-gov.github.io/schemas/boundary.unkeyed.schema.json",
  "$schema_version": "1.0",
  "$comment": "Hans v2 denominator sidecar. Empty `dropped` with totals.dropped=0 is the canonical 'perfect snapshot' signal — written explicitly so downstream readers never have to distinguish 'no drops' from 'no sidecar'.",
  "for": "S22-subdistricts.geojson",
  "totals": {
    "original": 300,
    "retained": 300,
    "dropped": 0
  },
  "dropped": []
}
