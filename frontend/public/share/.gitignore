# Generated by `bun run build:cards` (R7 of
# TODO/20260615-state-election-event-page-redesign-plan.md).
#
# One 1200x630 PNG per (state, event_id) keyed at
# `share/{state-slug}/{event_id}.png` (and `share/national/{event_id}.png`
# for scope=national rows). The PNGs ship as static OG-card unfurl
# assets but are NOT checked in - the build step regenerates them on
# every `bun run build` from the canonical event_summary.csv +
# parties.csv + state_codes.csv inputs, so the bytes are derived
# data per CLAUDE.md section 3 (`.runtime/` ephemeral discipline,
# applied to build artefacts here).
*.png
!README.md
