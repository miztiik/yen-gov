"""Derived canonical read models.

Derived modules materialise small route-shaped CSVs from canonical source
CSV files. They are local pipeline products only; the static frontend reads
their emitted CSV contracts at runtime.
"""
